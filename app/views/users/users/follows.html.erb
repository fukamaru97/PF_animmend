<div class="container">

  <div class="follows mt-3">
    <h4>フォローしているユーザー</h4>
      <% @users.each do |user| %>
        <%= image_tag user.get_profile_image(50,50) %>
        <%= link_to user.name, user_path(user) %>
      <% end %>
  </div>

</div>