<h1>作品登録</h1>
 <%= form_with model: @work, url: admins_works_path, method: :post do |f| %>
  <h4>作品画像</h4>
   <%= f.file_field :image, accept: "image/*" %>
  <h4>作品名</h4>
   <%= f.text_field :name %>
  <h4>ストーリー</h4>
   <%= f.text_area :story %>
  <%= f.submit '登録する' %>
<% end %>